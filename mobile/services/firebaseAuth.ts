// Firebase is not used. Authentication is purely JWT-based via backend OTP + password flows.
export const firebaseAuth: any = null;
export const isFirebaseAvailable: boolean = false;

export const setConfirmationResult = (_result: any) => {};
export const getConfirmationResult = () => null;
